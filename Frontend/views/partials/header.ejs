<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="icon" href="/photos/VASTRAKART.jpg" type="image/jpeg">
    <title><%= typeof title !== 'undefined' ? title : 'Vastrakart' %> | Vastrakart - Your Bhilwara Fashion Guide</title>
    <link href="https://fonts.googleapis.com/css2?family=Urbanist:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/css/index.css"> 
    <link rel="stylesheet" href="/css/contact.css">
    <link rel="stylesheet" href="/css/login.css">
    <link rel="stylesheet" href="/css/storeDetail.css">
    <link rel="stylesheet" href="/css/dressDetail.css">
    <link rel="stylesheet" href="/css/footer.css">
</head>
<body>
    <div id="preloader">
        <img src="/photos/VASTRAKART.jpg" alt="Vastrakart Logo" class="preloader-logo" />
    </div>

    <% if (success && success.length > 0) { %>
        <div class="flash-message success">
            <%= success.join(', ') %>
        </div>
    <% } %>
    <% if (error && error.length > 0) { %>
        <div class="flash-message error">
            <%= error.join(', ') %>
        </div>
    <% } %>
    <% if (info && info.length > 0) { %>
        <div class="flash-message info">
            <%= info.join(', ') %>
        </div>
    <% } %>

    